# Force-Kill-Live-Session
If an attacker steals a password and is clicking around inside the actual AWS Web Console, deactivating API keys won't stop them. This script instantly deletes the user's login profile and revokes their active browser session.
